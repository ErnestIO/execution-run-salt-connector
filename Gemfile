source 'https://rubygems.org'

gem 'salt', path: '/opt/ernest-libraries/salt/'

group :development, :test do
  gem 'pry'
end

group :test do
  gem 'rspec'
  gem 'rubocop',   require: false
  gem 'simplecov', require: false
end
